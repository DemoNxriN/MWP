services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: SJdnan4Hu59yeStSvh87gDn3hwbJr
      MYSQL_DATABASE: dbmwp
      MYSQL_USER: mwp
      MYSQL_PASSWORD: Y6rvrTykzPE6jP4a0yrRr2NBVX43
    volumes:
      - db_data:/var/lib/mysql
      - ./db_schema.sql:/docker-entrypoint-initdb.d/db_schema.sql
    networks:
      - mwp


#Backend
  api:
     build: ./api
     container_name: backend_api
     ports:
       - "5000:5000"
     environment:
       DB_HOST: db
       DB_USER: mwp #Cambio esto de clientmwp a mwp porque el usuario de la base de datos es mwp, cualquier cosa se comenta y se cambia de nuevo si es necesario.
       DB_PASSWORD: Y6rvrTykzPE6jP4a0yrRr2NBVX43
       DB_NAME: dbmwp
     depends_on:
       - db
     networks:
       - mwp


#Frontend
  web:
    image: nginx:alpine
    container_name: frontend_web
    volumes:
      - ./web:/usr/share/nginx/html:ro
    ports:
      - "8080:80"
    # depends_on:
      # - api
    networks:
      - mwp

volumes:
  db_data:

networks:
  mwp:
